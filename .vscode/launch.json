{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (Always New Empty Window)",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "--new-window"
      ]
    }
  ]
}
